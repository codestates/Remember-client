export type dummyList = {
  id: number;
  title: string;
  die: string;
  date: string;
  img: string;
};

export type sponsor = {
  id: number;
  number: string;
  avatar: string;
  name: string;
  money: string;
  donation: string;
};
